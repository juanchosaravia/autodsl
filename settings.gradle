pluginManagement {
    repositories {
        maven { url 'https://plugins.gradle.org/m2/' }
        mavenCentral()
    }
}
rootProject.name = 'core'

include 'app'
include 'annotation'
include 'processor'